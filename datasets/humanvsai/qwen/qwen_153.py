def __findRange(self, excelLib, start, end):
    low = excelLib[start]
    high = excelLib[end]
    return (low, high)

def __findRange(self, excelLib, start, end):
    """return low and high as excel range"""
    low = excelLib[start]
    high = excelLib[end]
    return (low, high)