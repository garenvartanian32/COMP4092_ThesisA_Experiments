def get_token_node_ids(sentence: str) -> list:
    # tokenize the sentence
    # create an empty list to store the token node IDs
    # iterate through the tokens
        # get the node ID for the current token
        # append the node ID to the list of token node IDs
    # return the list of token node IDs
    pass
