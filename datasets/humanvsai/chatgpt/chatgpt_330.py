def create_interface_menu(panel, point=None):
    if point is None:
        point = panel.cursor().pos()
    # create interface menu
    # ...
    # return the interface menu
    return interface_menu
