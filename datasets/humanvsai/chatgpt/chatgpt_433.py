def check_mouse_over_widget(event, include_label=False, width_modifier=0):
    if include_label:
        #logic to check mouse event over widget including label
        pass
    else:
        #logic to check mouse event over widget excluding label
        pass
    return True #or False depending on the outcome of the above logic
