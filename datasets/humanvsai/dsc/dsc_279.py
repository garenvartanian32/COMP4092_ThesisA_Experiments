def toggle_save_cb(self, w, res_dict):
    """Only enable saving if something is selected."""
    if w.state == 'selected':
        # Enable saving
        pass
    else:
        # Disable saving
        pass